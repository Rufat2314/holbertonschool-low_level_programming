#include "main.h"
/**
 * add - checks for lowercase character.
 * @n: The character to be checked.
 * @m: adds two integers and returns the result.
 *
 * Return: Always(Success)
 */
int add(int n, int m)
{
	return (n + m);
}
